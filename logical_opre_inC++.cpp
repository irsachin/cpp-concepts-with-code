#include<iostream>
using namespace std;
int main()
{
   int x =5;
   int y=10;
   //logical && (and )both the condition should be right
   //0 means false statement ,1 true statement 
   cout<<"logical operator && (and) = "<<(x>10 && y <15)<<endl; // false statement 
   cout<<"logical operator && (and) = "<<(x>3 && y <15);   // true statement 
   // logical || (or) 

}